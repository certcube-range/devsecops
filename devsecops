pipeline {
  agent any
  tools {
    maven 'maven'
    }
    stages {
    stage ('initalize') {
      steps {
        sh '''
                echo  ""PATH = ${PATH}"
                echo  "M2_home = ${M2_HOME}"
                
        }
      }
      
     stage ('Build') {
        steps {
        sh 'mvn clean package'
        }
       }
       
  }
  
}
